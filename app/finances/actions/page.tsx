import { FinancePlaceholder } from "@/components/finance-placeholder";

export default function FinanceActionsPage() {
  return (
    <FinancePlaceholder
      title="Actions"
      note="Not fully sure yet what this should contain — a reminders/task list for financial to-dos (quarterly tax, subscription renewals)? Confirm and it gets built next."
    />
  );
}
