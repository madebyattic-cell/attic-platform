import { FinancePlaceholder } from "@/components/finance-placeholder";

export default function ExpensesPage() {
  return (
    <FinancePlaceholder
      title="Expenses"
      note="You confirmed you want real expense tracking — nothing exists in the database for this yet. Next session: a real expenses table, a manual entry form, and category breakdowns."
    />
  );
}
