import { FinancePlaceholder } from "@/components/finance-placeholder";

export default function IncomePage() {
  return (
    <FinancePlaceholder
      title="Income"
      note="Your real revenue already exists on the Overview page. A dedicated Income view with date ranges and channel breakdowns is next up — just needs to be built."
    />
  );
}
